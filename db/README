ssts.sql creates the history database, the users database, and the stocks
database.

It's probably not normalized, but it's probably not worth looking
into.

All fields in the stock database are VARCHARS, because Yahoo Finance
sets fields to N/A if no data is available.
